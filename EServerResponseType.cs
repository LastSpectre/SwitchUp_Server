namespace BA_Praxis_Library
{
    public enum EServerResponseType
    {
        // nothing was found
        NONE = 0,

        // request was successful
        OK = 1,

        // error occured
        ERROR = 2
    }
}